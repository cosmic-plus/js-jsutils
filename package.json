{
  "name": "@cosmic-plus/jsutils",
  "version": "1.15.0",
  "description": "Generic JavaScript utilities that are not related to Stellar",
  "author": "MisterTicot",
  "repository": "github:cosmic-plus/node-jsutils",
  "license": "MIT",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prettier": "prettier --write --no-semi 'src/*.js' '*.json' '*.md'",
    "eslint": "eslint --fix 'src/*.js'",
    "lint": "npm run prettier && npm run eslint"
  },
  "devDependencies": {
    "eslint": "^5.16.0",
    "prettier": "^1.16.4"
  }
}
